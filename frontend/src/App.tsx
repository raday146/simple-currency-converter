import { CurrencyConverter } from './components/CurrencyConverter';

export default function App() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center px-3 py-6 sm:px-4 sm:py-10">
      <CurrencyConverter />
    </main>
  );
}
